# 📋 待办事项 — 基于全量代码审计重建

**更新时间**: 2026年7月27日
**基线提交**: `291744a` — feat: import thesis learning materials and weave confusable words into the main flow
**数据来源**: 22 个 agent 两轮审计（六维度并行调查 → 逐条对抗式验证），716 次工具调用，36 条发现**全部经过独立复核**
**上一版**: [docs/archive/待办事项-2025-11-22-归档.md](../archive/待办事项-2025-11-22-归档.md) — 停留在 2025-11，仅作历史记录保留

> **本文所有条目均已复核**：第二轮 8 个验证 agent 逐条到代码和数据库里复现证据，29/29 坐实、0 条被推翻，但**多条的严重程度、根因归属和行号被修正**——修正内容已写入正文，被夸大的因果关系已拆开或降级。

---

## 🎯 一句话结论

> **界面和路由是完整的，但「数据采集」这条链路从头到尾是断的。**
> 用户点了什么、学了几次、VKS 选了什么——一条都没真正落库。
> 更糟的是：**填空题的答案是预填给用户的**，正确率数据本身也是假的。

---

## ⚠️ 先读这一条：论文实验并没有被代码卡住

- 论文 §5.2 写明 **17 名被试已完整参与 2 个月教学实验**，第六章 SPSS 分析已完成
- 实验载体是 **PPT + 微信/WhatsApp**，§4.4 明确说明数据管道**按设计就是人工 Excel**
- **这套代码是实验的事后产品化，不是实验仪器**

**结论**：下面的排期**不需要按「救实验」的节奏推进**。
**例外**：如果要再跑一轮新的实证验证，P0 全部升级为必做，且实验分组标记必须在学习开始**之前**打上（事后无法从 `learning_session` 反推）。

---

## 🔥 P0 — 立刻做（4 条）

### P0-1 ｜ 埋点接口 100% 挂掉，过程数据全部丢失 `S` 🎓

**已实证复现**：把 db 拷到临时目录、用 `WORDS_DB_PATH` 指向副本后 POST `/api/learning/events/batch`，返回 **HTTP 500**：

```
(sqlite3.OperationalError) table learning_event has no column named target
INSERT INTO learning_event (session_id, event_type, target, event_data, page_url, timestamp, created_at)
```

| 位置 | 问题 |
|---|---|
| `app_phase.py:225` | 声明 `target = db.Column(db.String(100))`，真实列是 `event_target` |
| `app_phase.py:523` | `target=event.get('target')` |
| 对照组 | `models_extended.py:174`、`app_extended.py:143`、`scripts/migrate_database.py:205` **全部用 `event_target`**——只有 app_phase.py 写错 |

**没有救场路径**：`db.create_all()`（`app_phase.py:1707`）不会改动已存在的表；单条事件的 try/except 也救不了——异常发生在循环外的 `db.session.commit()`，直接落到外层 except → 500。

**连带问题**：`utils/timeTracker.ts:299-310` 在 `success=false` 时把整批事件重新塞回队列（`this.events = [...eventsToSend, ...this.events]`），由于永远失败，队列无限膨胀。

**第一步**
1. `app_phase.py:225` → `target = db.Column('event_target', db.String(100))`（保留属性名，显式指定列名），同步 `:523`
2. `utils/timeTracker.ts:305` 加最大重试次数或队列上限
3. 用 `WORDS_DB_PATH` 指向 db 副本跑一次 test_client POST，确认返回 200 且行落库

---

### P0-2 ｜ `user_progress` 永远没有行，SM-2 与论文特征全部落空 `M` 🎓

> ⚠️ **根因已修正**：不是「app_phase.py 只读不写」。`app_phase.py:478` 确实调用了 `spaced_repetition.update_user_progress_after_exercise`——问题是 `adaptive_engine.py:626-643` 里它是**纯 UPDATE**，没有对应行时静默影响 0 行，且 `app_phase.py:479-481` 的 try/except 把任何异常都吞掉。**既不报错也不落库。**

**更进一步**：即使补了 get-or-create，那条 UPDATE 也只写 5 列（`last_studied` / `next_review_suggested` / `review_count` / `consecutive_correct` / `consecutive_incorrect`）。全仓 grep 显示**没有任何生产代码**写论文表 4.3 需要的特征列：`total_attempts` / `correct_attempts` / `total_study_time_seconds` / `definition_attempts` / …

唯一的 `UserProgress(...)` 构造在 `app_extended.py:389`（PM2 跑的是 app_phase.py，用不到）和 `tests/test_adaptive_engine.py:116` 的测试夹具。

**数据佐证**：`user_progress` 只有 10 行，全是 `test_user_001~010`、`word_id` 均为 1。

**第一步**
1. 在 `record_exercise`（`app_phase.py:446-486`）的 `db.session.commit()` 之后、调 `spaced_repetition` 之前，按 `(session.user_id, session.word_id)` 做 get-or-create
2. 同一处按 `data['questionType']` 累加 `total_attempts` / `correct_attempts` 和对应的 `definition_attempts` | `collocation_attempts` | `fill_word_attempts` 及其 `_correct`
3. session 结束的写入点补 `total_sessions` 与 `total_study_time_seconds`
4. 给 `user_progress` 加 `(user_id, word_id)` 唯一索引（现表没有），或把 `adaptive_engine.py:627` 改成 `INSERT ... ON CONFLICT DO UPDATE`

> **这条才是 SM-2 真正的阻断点**，不是 numpy（见下方「已修正的判断」）。

---

### P0-3 ｜ VKS 初始档位按「学习者 × 词 × 轮次」落库 `M` 🎓

`utils/timeTracker.ts:8-13` 的 `TimeTrackingConfig` **类型层面就没有 `initialLevel` 字段**，`:97-105` 的 startSession 请求体也不发它，所以 `app_phase.py:378` 的 `data.get('initialLevel')` **恒为 None**——论文表 4.3 的头 5 个特征没有数据源。

目前唯一的 VKS 落点是 `user_profile.current_vks_level` 单列，是**覆盖语义**：学第二个词就把第一个词的档位冲掉了。

**第一步**
1. `TimeTrackingConfig` 加 `initialLevel?: string`，startSession body 加 `initialLevel: this.config.initialLevel`（后端已就位）
2. 五个模块页各加 `initialLevel: learningSession.vksLevel ?? undefined`：
   `character-learning.tsx:20-24`、`word-learning.tsx:19`、`collocation-learning.tsx:20`、`sentence-learning.tsx:18`、`exercise.tsx:59`
3. **⚠️ 关键第三步别漏**：`hooks/useTimeTracking.ts:47` 的依赖数组必须加 `config.initialLevel`，否则 vksLevel 从 localStorage 水合前 tracker 已发出 startSession，仍然写 NULL
4. 再建不可变表 `word_attempt(user_id, word_id, round_no, vks_level, entry_module, created_at)`，在 `word-learning-entrance.tsx` 的 `handleContinue` 里 `router.push` 前 **await** 一次显式 POST

> **不要走 trackEvent 批量通道**：实测 `vks_test_complete` 在 `learning_event` 表里 0 行。

---

### P0-4 ｜ 练习题效度：正确率数据目前是假的 `S` 🎓

> 🚨 **本条根因排序已修正**，最严重的一环是第二轮才发现的：

#### ① 手写题把正确答案预填给用户 —— 最严重

```
components/HandwritingInput.tsx:74
  const userInput = prompt('请确认您手写的内容：', expectedAnswer || '');
pages/exercise.tsx:458
  expectedAnswer={currentQuestion.correctAnswer}
```

确认弹窗的**默认值就是正确答案**。用户随便画一笔点确定就 100% 判对，写进 `exercise_record` 的 `is_correct` **恒为 true**。填空题实际上不采集任何有效作答信号。

#### ② 填空题从「单字填空」退化成「整词书写」

`app_phase.py:668-682` 给非 1 号词的 fill_word 设 `correctAnswer = base_hanzi`（整词），而论文原型题（`app_phase.py:606-611`）的 `correctAnswer` 是单字 `生`。
（注：fill_word 前端不渲染选项按钮，走 `exercise.tsx:445-462` 的 HandwritingInput 分支，后端塞的 `options` 字段根本没用上。）

#### ③ 双向子串匹配判分

`pages/exercise.tsx:241-245`：`correctAnswer.includes(userAnswerLower) || userAnswerLower.includes(correctAnswer)`
正确答案 `happen; occur` 时，只输入 `a` 也判对。

#### ④ 干扰项混入无素材词

`build_options()`（`app_phase.py:570-587`）不做素材过滤。**修正数字**：真正会产出畸形选项的是 **11/70** 个候选词（不是 14）——id=4 是纯拼音 `fāshēng`，id 6-15 是汉字+拼音混排（`突然 tūrán`、`二 èr`）；id 2/3/5 的 pinyin 列存的就是纯汉字，正常。
损害的是**测量效度**：格式一眼不像答案，四选一退化成二三选一。
另：`app_phase.py:589-590` 连续调用了两次 `build_options()`，第一次结果被直接丢弃。

**第一步（按严重度排序）**
1. `HandwritingInput.tsx:74` 的 prompt 默认值改成空串
2. `exercise.tsx:243-245` 改严格相等（先 trim + 全角半角归一）
3. `app_phase.py:668-682` 仿照 `606-611`，用 Character 表取一个字挖空，`correctAnswer` 设为该单字
4. `build_options()` 候选查询加 `Word.id.in_(db.session.query(Character.word_id).distinct())`，并删掉 `:590` 那行重复调用

---

### 🚨 P0 期间的数据操作红线

- **不要**删除 word id **6-15** —— `confusable_pairs` 1-5 引用它们
- **不要**单独把汉字从它们的 pinyin 列拆出去，除非同一 PR 已改完 `confusable_api.py`
- 可安全 DELETE 的只有 id **2 / 3 / 4 / 5**（已验证 `user_progress` / `learning_session` 零引用）

---

## 📊 P1 — 接下来

### P1-1 ｜ 认证：先补 schema，**绝不能先收紧装饰器** `M`

**两个问题叠加，顺序搞反会锁死所有人。**

**问题 A — auth 根本跑不起来**：`words_extended.db` 里没有 `user_session` 表，`user_profile` 也没有 `email` / `password_hash` 列（实测三条查询全部 `no such table` / `no such column`）。`auth.py:57/74/148/331` 全依赖这些缺失 schema，所以 `/api/auth/register` 和 `/api/auth/login` **必然 500**。`scripts/migrate_auth.py` 是唯一建表加列的地方，但只有 `tests/test_api.py` 调用过，部署链路从不执行。

**问题 B — 身份可冒充**：`user_data_isolation.py:38-50` 把 view_args / query / body 里的 `user_id` 直接当身份返回（`is_authenticated=False`）。`app_phase.py` 恰好 **9 处** `@require_authentication(allow_url_param=True)`：`357, 397, 432, 803, 926, 986, 1023, 1051, 1076`。端点内 `if user_id != current_user_id` 两边同源恒假。`/api/users` 连装饰器都没有。

**第一步（顺序不能反）**
1. 在 `initialize_database()` 的 `db.create_all()` 之后**幂等调用** `migrate_auth_system(_db_path)`，让注册/登录先能用
2. 给 `auth.py` 的 `validate_session` 和 `user_data_isolation.py:31-36` 包 `except sqlite3.Error`
3. **之后**才把那 9 处改 `allow_url_param=False`，`/api/users` 加认证
4. `components/UserSwitcher.tsx` 从 `pages/_app.tsx:35` 移除，或限制为 `NODE_ENV !== 'production'`

> 先收紧装饰器 = 所有人被锁在门外，因为登录本身跑不通。

---

### P1-2 ｜ 复习次数规则（论文表 4.2）与 SM-2 冲突 `L` 🎓

论文 §4152-4205「假定学习者至少需要重复 7 次」，表 4.2 按 VKS 给**次数**：A/B → 搭配与填词各 5 次，C → 4，D → 3，E → 2，做错各加 1 次。

代码只有 `adaptive_engine.py:33` 的 `base_intervals = [1, 3, 7, 14, 30, 90, 180, 365]`（**行号更正**：`:32` 是注释），天级间隔计算在 `:403-465`。论文 §5456-5468 的节奏是 2 周学 + 2 周复习，SM-2 的 14/30/90/180/365 天在这个窗口里毫无意义。

代码侧零实现：`review_count` 只在 `pages/today-review.tsx:16/161` 被显示成 "Reviewed N times"，`user_progress` 只有计数列，没有 target/remaining。

**第一步**：先补 P0-2 的 upsert（否则计划表拿不到行）→ 在 `word-learning-entrance.tsx:183` 提交 `vks_level` 时按表 4.2 生成 `word_review_plan(user_id, word_id, vks_level, question_type, target_times, done_times)` → 把 `/api/review/user/<id>/due` 的排序键改成 `target_times - done_times > 0`。

---

### P1-3 ｜ 181 道人工命题零导入 `L` 🎓

`scripts/import_learning_materials.py:327` 的 `if path.name.startswith('_') or 'answer' in path.name.lower(): continue` 跳过 6 个答案课件。

**181 这个数字已独立复现**：65 张 practice slide = Find responding 65 + Find right collocation 52 + Fill in right word 64。

> ✅ **好消息（第二轮才发现）**：这 6 份答案课件**已经被解析成 JSON 躺在 `data/learning_materials/` 里**，含正确选项字母和 A/B/C/D 干扰项——**parser 的输入是现成的，不需要重新解 pptx**。

**第一步**：新增 `scripts/parse_answer_decks.py` 读 `data/learning_materials/the answer*.json`，按三个题干切块 → 建 `exercise_item(word_id, deck, item_type, stem, options_json, correct_answer)` → `app_phase.py:547` 的 `get_word_exercises` 改为优先查 `ExerciseItem`，查不到才回落随机逻辑。可用 word_id=1 比对硬编码原型题验证解析正确性。

---

### P1-4 ｜ 易混淆词：8 组无汉字 + 5 组不可达 `S`

> ⚠️ **因果关系已修正**：这**不是**「word 表 14 行无素材」造成的，恰好相反。

| | 现象 | 真实原因 |
|---|---|---|
| pair **6-13**（8 组）| 页面上只有拼音 `gǎibiàn` vs `biànhuà`，AudioPlayer 还用中文 TTS 念拉丁字母 | 这些词在 character 表里**都有汉字**；是 `confusable_api.py:26-27` 和 `:104-105` 的 SQL 只 SELECT `w1.pinyin`，返回体（`:55-63`、`:126-135`）没有 hanzi 字段 |
| pair **1-5**（5 组）| 主线学习流里永远到不了 | 它们用的 word id 6-15 在 character/collocation/example 三张表里都是 0 行；而两条推荐路径都硬性要求有例句（`app_phase.py:1198-1202` 的 `Word.id.in_(has_materials)`、`adaptive_engine.py:192` 的 `EXISTS(SELECT 1 FROM example ...)`）|

讽刺的是：**pair 1-5 反而是页面上唯一能看到汉字的 5 组**，因为它们的 pinyin 列里混了汉字。

**第一步**：`confusable_api.py` 两处 SQL 加 `(SELECT GROUP_CONCAT(c.character,'') FROM character c WHERE c.word_id=cp.word1_id ORDER BY c.id) AS word1_hanzi`，返回体加 `hanzi` 字段并以 pinyin 兜底；`confusable-words.tsx:128-130/145-147` 主标题渲染 hanzi、pinyin 降副标题，AudioPlayer 改传 hanzi。可达性另修：给 word 6-15 补 example/character 行，或放宽两处 EXISTS 硬过滤。

---

### P1-5 ｜「今日复习」是一次性死路 `M`

`index.tsx:32` 在 `dueCount > 0` 时把用户送进 `/today-review`，但 `today-review.tsx:52-69` 跳走后并入线性主线，`exercise.tsx:318-322` 结束直接去 dashboard——**剩余 N-1 个到期词静默丢弃**。附带：`selectedReview` state 设了从不读；`recommended_module: 'review'` 在 `moduleRoutes` 里没有 key，卡片显示英文 `review`，标题显示 `fāshēng`。

**第一步**：`LearningSessionContext` 加 `reviewQueue` / `reviewIndex`。

---

### P1-6 ｜ 练习加载失败静默换词 `M`

`exercise.tsx:112-115` 失败时递归回退到词 1，而 `:99` 的成功路径会 `updateLearningSession` 把整个会话的词覆盖成「发生」并落盘 + 同步后端——**用户前四个模块学的词，练习记录被记到词 1 头上**。

**第一步**：把 `updateLearningSession` 从 `loadExercises` 内部提出，仅在 `data.wordId === desiredWordId` 时写。

---

### P1-7 ｜ 第一课的原型硬编码 `S`

真实位置是 **`app_phase.py:593-633`**（行号更正，不是 591-630）。

> **两处已修正的夸大**：① 题干本身（`容易 ( )`、`不愿意 ( ) 的事情终于出现了。`）与 DB 里 word 1 的真实材料几乎一致，只有干扰项「嘴/哪儿/第一」是原型残留；② 「每个新用户」只在 fallback 路径成立（CLAUDE.md 说明生产走的正是这条），若 adaptive_engine 正常加载，`ORDER BY mastery ASC, RANDOM()` 下命中 word 1 的概率约 1/56。

**⚠️ 删除前必须回归验证**：4 道原型题里的 `choose_word` **只在这个分支里出现**，直接删会让 word 1 的题型比现在还少。

---

### P1-8 ｜ 测试覆盖 `M`

- **后端 61 个用例跑在只有 1 个词的玩具种子库上**，6 套论文教材与新主流程接口零覆盖
- **E2E journey 测试对「辨析步骤」无断言**——该功能整体回归时测试仍然 PASS
- **前端零单元测试**（`devDependencies` 只有 `puppeteer-core`，`npm test` 实际是 `python3 -m pytest tests -q`）

> ⚠️ **「路由完全裸奔」已推翻**：`tests/e2e/smoke.test.js` 覆盖了 19 条路由渲染、`HOME_NAV_TARGETS`（5 个首页入口落点）、`CHAIN_PAGES`（5 个学习页前后导航），还有老用户 localStorage 水合回归，且曾抓到 3 个真实 Bug。缺的是**快速、无需起服务的单元层**。

**第一步**：只装 vitest（纯逻辑测试连 jsdom 都不需要），把 `exercise.tsx:236-250` 的判分抽成 `utils/grading.ts` 的纯函数 `judgeAnswer()`，为它写第一批用例（大小写、前后空格、`happen; occur` vs `a` 的子串误判）；再补 `useLearningNavigation.ts` 的 `normaliseModule` / `moduleOrder`。其余页面渲染继续交给 smoke.test.js。

---

### P1-9 ｜ CI 止血 `S`

`.github/workflows/pr-quality.yml:3` 是 `pull_request_target`，`:17` checkout `head.sha`——把外部 PR 代码检出进持有 `OPENAI_API_KEY` 和 `pull-requests: write` 的特权 workspace（仓库 public，CONTRIBUTING 邀请 fork PR）。

**第一步：删掉 `with: ref:` 块，且绝不要往这个 workflow 里加任何构建步骤**（加一步 `npm ci` 就等于 RCE）。之后新建独立 `ci.yml`（`on: pull_request`）跑 pytest / `tsc --noEmit` / `next build`。

> pytest 放 `requirements-dev.txt`，别进 `requirements.txt`——Dockerfile 会打进生产镜像。

---

### P1-10 ｜ 数据清洗 `M`

| 对象 | 问题 |
|---|---|
| 搭配（94 条）| 86 条残留「（一级/二级/三级）」标注；35 条按 `collocation-learning.tsx:158` 会把词头显示两遍（「改变 颜色(二级)改变」）；3 条不含词头，`app_phase.py:652` 的 replace 挖不出空 → 生成不可答的题 |
| 单字（110 个）| 19 个 pinyin 为空；12 个把 `hòu NOUN climate` 这类原始标记泄漏进释义列 |

导入器的 `--report` 只报出 1 个问题，是**虚假绿灯**——修数据的同时要修 report 逻辑。

---

## 🎨 P2 — 有空再说

- **numpy 清理** `S` — `adaptive_engine.py:14` 的 `import numpy as np` 全文未使用（已复核，0 处调用），删掉即可。注意 `scripts/generate_test_data.py:11` **确实在用**（`:360` 的 `np.random.poisson`），若保留该脚本需把 numpy 写进 `requirements-dev.txt`
- **README 修复** `S` — README_zh.md 第 39-72 行的 10 张截图路径全部失效（README.md 的 9 张全部正常，按它替换即可；第 10 张 `learning-stats.png` 无对应文件，需补截图或删格）；Tech Stack 表第 90-91 行把论文的 ML Models / NLP Pipeline 与 Frontend/Backend 并列，需移入 Research Foundation 或加限定语；版本号**三方分叉**：README.md `2.2.0` / README_zh.md `2.0.0` / package.json `1.0.0`；README_zh 末尾三个断链需指向 `docs/core/`
- **一键启动修复** `S` — `start_system.sh:11` 的 `python simple_test_data.py` 文件已搬到 `scripts/`，且用的是 `python` 而非 `python3`。**更狠的一点**：即使路径改对也会失败——`scripts/simple_test_data.py` 只 CREATE 了 `learning_event`（`:124`），却直接 INSERT 进 `user_profile`/`learning_session`/`exercise_record`/`user_progress`（`:27/53/74/96`），全新克隆时报 `no such table`。正确修法是**改路径 + 调换顺序**（先起后端建表再灌种子）
- **实验编排** `L` — 分组字段、前后测页、导出接口。**不急**，见开头说明
- **Docker 链路** `S` — `Dockerfile:26` COPY 一个被 gitignore 的 `*.db`；未 COPY `scripts/`
- **死代码清理** `S` — `pages/c.tsx`（零引用 device mockup，违反 CLAUDE.md）；`app.py` / `app_extended.py` / `app_simple_extended.py`（41KB 僵尸后端）；`tests/test_time_tracking.js`（`node-fetch` MODULE_NOT_FOUND，`:228` 会直接崩）；未引用的 `recharts` 依赖；`/time-tracking-demo` 从首页移除（按钮会往真实后端写 `demo_user`/`wordId 999` 的假记录）
- **`/api/adaptive/feedback` 前端零调用** `M` — `adaptive_recommendation` 的 `is_accepted` / `actual_choice` / `effectiveness_score` 三列 151/151 全 NULL；`confusable_exercise_records` 0 行
- **模拟数据标记** `S` — 全库行为数据 100% 为单批模拟且全部落在 `word_id=1`，与将来的真实数据混在一起会污染分析，建议加 `is_synthetic` 标记列

---

## 🔢 建议执行顺序

| # | 任务 | 为什么在这个位置 |
|---|---|---|
| 1 | **P0-1** 埋点列名 | 零依赖、改动最小。**必须第一**——后面每一步的验证都要靠埋点真的在写 |
| 2 | **P0-4 ①** 手写题答案预填 | 一行改动，但它决定从此刻起每一条 `is_correct` 是否可信。**越早越好** |
| 3 | **P0-4 ②③④** 判分与题型 | 与 1 独立，可并行 |
| 4 | **P0-2** user_progress upsert | 依赖 2/3（判分修好后累加的 correct 才有意义）。**这条才是 SM-2 的真正解锁点** |
| 5 | **P1-1 前半**（跑 migrate + 包 try/except）| 纯增量、零破坏，且**必须在任何 cookie 出现之前**完成 |
| 6 | **P0-3** VKS 落库 | 放在 4 之后：需要 P0-2 建立的 `user_progress` 行做交叉校验 |
| 7 | **P1-1 后半**（关 `allow_url_param`、移除 UserSwitcher）| **必须最后**——它改变所有请求的认证语义 |
| 8 | **P1-9 CI 止血** | 可在任意时刻插入，删掉 `with: ref:` 块只要 30 秒 |
| 9 | 其余 P1 | P1-2 → P1-3 → P1-4 → P1-5/6/7 → P1-8/10 |

**核心依赖链**

```
user_progress upsert → SM-2 生效 → 复习调度可用 → 复习次数规则(P1-2) 才有落脚点
                    ↑
        判分修复(P0-4) 保证累加进去的数是真的
```

---

## 🎓 论文必需 vs 🔧 工程质量

### 🎓 直接影响论文有效性 / 数据可复现

| 任务 | 对应论文位置 |
|---|---|
| P0-1 埋点 500 | §4.4 过程性数据源 |
| P0-2 `user_progress` 落库 | 表 4.3「学习次数 / 总时长 / 各题型正误」 |
| P0-3 VKS 按词按轮次落库 | 表 4.3 的头 5 个特征 |
| **P0-4 ① 手写题答案预填** | **§6.3 填词题习得分析——目前 `is_correct` 恒为 true，该章结论的数据基础不存在** |
| P0-4 ②③④ 题型与判分 | §6.3 正确率被系统性高估 |
| P1-2 复习次数规则 | 表 4.2 |
| P1-3 181 道人工命题 | 论文教学设计核心 |
| P2 实验分组字段 | **仅当决定再跑新实验时**升级为必需 |

### 🔧 纯工程质量

- **P1-1 认证** — ⚠️ **例外**：若要再招真人被试，身份可信度直接决定数据有效性 → 那时升为论文必需
- **P1-4 易混淆词显示** — 灰区：不影响已完成的实验，但影响新数据的教学有效性
- **P1-5 / P1-6** — 复习队列与静默换词（P1-6 有轻度数据污染）
- **P1-8 测试 / P1-9 CI / P1-10 数据清洗 / P2 全部**

---

## ❌ 已被推翻或修正的判断

复核推翻了这些说法，**不要据此排优先级**：

1. **「论文实验被代码卡住了」——不成立。** 实验已完成（17 人 2 个月，SPSS 分析已出），代码是事后产品化。
2. **「删掉 numpy 就能让 SM-2 跑起来」——因果错误。** `adaptive_engine.py:14` 的 import 确实全文未用、requirements 里也确实没有 numpy（干净镜像会走 fallback），但**即使删掉、引擎正常加载，SM-2 对真实用户依然不生效**——真正的阻断点是 `user_progress` 没有行可 UPDATE（P0-2）。numpy 因此从 P0 降到 P2 清理项。
3. **「word 表 14 行无素材导致辨析词对不显示汉字」——因果完全相反。** 8 组无汉字的 pair 6-13 在 character 表里都有汉字，根因是 `confusable_api.py` 的 SQL 只 SELECT pinyin；而无素材的 word 6-15 对应的 pair 1-5 反倒是唯一能看到汉字的。两件事成因相反，已拆成两条。畸形干扰项的真实规模也是 **11/70** 而非 14。
4. **「填词题退化为整词选择」——措辞错误。** fill_word 前端不渲染选项按钮，走 HandwritingInput 分支，后端的 `options` 字段根本没用上。准确说法是「单字填空退化成**整词书写**」。
5. **「前端路由完全裸奔」——不成立。** `tests/e2e/smoke.test.js` 覆盖 19 条路由 + 导航落点 + 水合回归，曾抓到 3 个真实 Bug。缺的是单元层。
6. **「新克隆者拿不到任何数据库」——夸大。** `app_phase.py:1793` 在模块导入时无条件 `initialize_database()`，会建表并插入示范词；`data/learning_materials/` 下 12 个 JSON 也在版本控制里，配导入脚本可完整重建。真正的问题是 README 从未提这条路径。
7. **「论文第四章算法未实现 = blocker」——降为 high。** 论文 §4067-4080 把 ML 算法限定在**测试组**，训练组用的就是纯自适应规则——所以当前的纯规则实现本身是论文中一个**合法的实验条件**，不是功能性崩坏。且复现需要 65 词 × 学习者的训练数据，仓库里没有。
8. **「prguard 正在泄密」——夸大。** 它是仓库属主自己账号下的 action（`1137043480/prguard`），跑的是 action 自带 bundle，不执行 workspace 代码。是**潜伏隐患**——离 RCE 只差往那个 job 里加一步 `npm ci`。
9. **「eslint 的 26 个 error 藏着真 bug」——不成立。** 全量枚举：12 个引号未转义、5 个 `react/react-in-jsx-scope`（Next.js automatic runtime 下的误报）、4 个 `no-explicit-any`、3 个 `semi`、2 个 `ban-types`——**零个正确性缺陷**。别急着把 eslint 加进 CI 门禁。
10. **「word 表缺 hanzi 列是根因」——不是。** `app_phase.py:239-242` 早有 `get_word_hanzi()` 且带兜底。根因是 `confusable_api.py` 作为独立注册模块没接到这个 helper。

---

## 📈 进度追踪

| 优先级 | 任务数 | 🎓 论文必需 | 🔧 工程质量 |
|--------|-------|-----------|-----------|
| P0 | 4 | 4 | 0 |
| P1 | 10 | 2 | 8 |
| P2 | 8 | 0（1 条有条件）| 8 |
| **合计** | **22** | **6** | **16** |

> 上一版文档的「9/9 完成，技术完成度 100%」指的是**页面与路由**，这个判断本身没错；本版计入的是页面背后的数据管道与内容效度。两者不冲突，是不同层次。

---

## 🗂️ 相关文档

- 归档的上一版待办：[docs/archive/待办事项-2025-11-22-归档.md](../archive/待办事项-2025-11-22-归档.md)
- 开发文档：[docs/core/开发文档.md](开发文档.md)
- 测试文档：[docs/core/测试文档.md](测试文档.md)
- 论文原文：[docs/academic/毕业论文-康正伟-1601210580_20250929_103404.md](../academic/毕业论文-康正伟-1601210580_20250929_103404.md)
- 项目运行约定：[CLAUDE.md](../../CLAUDE.md)

---

**下次复审**: 完成 P0 全部 4 条后，重跑一次六维度审计验证数据管道是否真的通了
