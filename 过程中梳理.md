现状梳理

  - pages/word-learning.tsx:8、pages/collocation-learning.tsx:8、pages/sentence-learning.tsx:8 都是纯静态示例，词汇、搭配、例句直接硬编码为“发生”，没有从任何 API 拉取真实数据，
  也没有通过推荐结果驱动学习流。
  - 自适应与复习相关的前端入口集中在演示/运维页面：pages/phase2-demo.tsx:18 会按按钮顺序去测 buildApiUrl('/api/…')，pages/learning-stats.tsx:14 只展示 /api/stats 汇总数据，
  pages/system-status.tsx:26 用服务探针检测 API。它们证明后端接口可用，但并未把推荐结果写回学习界面。
  - 时间追踪 Hook (hooks/useTimeTracking.ts:14) 与工具类 (utils/timeTracker.ts:18) 会直接连到默认 http://localhost:5004 的第二阶段接口，因此“VKS 入口→练习”这条路径已经能把学习
  会话/题目数据写入阶段二数据库，但依旧使用静态题库。
  - 数据库里只有极少量样例词汇 (words_extended.db 查询结果显示只有 id=1 的“fāshēng”)，也佐证目前更多是演示用途。
  - 原型与完整设计文档已被转换成 Markdown(毕业论文-康正伟-1601210580_20250929_103404.md)，可以直接阅读；仓库里没有实际 PDF，但内容已经在该文件内。

  问题结论
  当前仓库已经具备 1) 第二阶段后端（推荐/复习/统计）和 2) 前端时间追踪框架，但学习界面仍停留在“测试版”：静态词卡 + 静态练习，既未根据推荐动态切换，也未把复习计划嵌入整条学习路
  径。还缺少用户体系、多词汇数据、复习入口等论文中描述的完整闭环。

  建议的下一步方案

  1. 打通词汇数据管道：实现 src/lib/apiClient 上的 fetchJson，新建 useWordData(wordId) Hook，用于 /word/<id>，在词卡/搭配/例句页面读取真实内容；并把 wordId 通过入口页存入
  localStorage 或 URL，确保子页面能按推荐切词。
  2. 接入推荐/复习逻辑：在 word-learning-entrance 中调用 /api/adaptive/recommendation/{user} 决定下一步模块，并把推荐记录（wordId + moduleType）作为上下文传入后续页面；为复习推
  荐增加专门的入口（例如 Dashboard 卡片跳转）。
  3. 统一会话上下文：抽象一个 LearningSessionProvider（React context）管理当前 sessionId、wordId、moduleType，让 useComprehensiveTracking 和各页面共享，避免每个页面都硬编码
  user123、wordId:1。
  4. 扩充学习内容与复习流程：补充数据库（或编写导入脚本）多词条数据；实现复习列表 UI，按 /api/review/user/.../due 展示并触发 SRS 流程；为练习页接入真实题目与判题接口。
  5. 完善用户与配置：引入简单登录/用户切换（可以先做伪账号选择），确保推荐、复习、统计都基于真实 user_id；补充 .env 与 README 说明如何配置 NEXT_PUBLIC_API_BASE_URL、如何启动阶
  段二服务。

  按以上步骤推进，可以把目前的“演示+静态页面”逐步演化成论文里描述的、由推荐驱动的完整学习 App。建议从步骤 1 开始，因为只要词卡与练习能读取真实数据，就能快速看见系统“活”起来；随
  后再接入推荐与复习，最终整合用户体系。
---

## 任务进度追踪

### Step 1：建立进度记录机制（进行中）
- 目标：在 `过程中梳理.md` 中持续记录阶段成果与下一步计划，支持后续回顾。
- 当前动作：补充本节，约定每完成关键步骤后同步更新，并在必要时使用 git 保存里程碑。
- 下一步：开始 Step 2，改造前端从 API 加载词汇数据，使学习页面支持动态内容。

### Step 2：词汇学习页面接入动态数据（完成）
- 动作：新增 `hooks/useWordData.ts`，扩展 `apiClient` 与类型定义；改造字符/词汇/搭配/例句四个学习页面从 `/word/<id>` 加载数据并处理加载、错误状态；在入口页持久化当前词汇 ID。
- 现状：页面已能从后端获取“发生”对应的字符、搭配、例句，等待后端补充更多词汇时可直接复用。
- 下一步：Step 3 打通推荐流程，串联入口页 → 推荐 API → 学习页面，并根据需要模拟学习记录以驱动推荐结果。

### Step 3：接入自适应推荐与学习数据模拟（完成）
- 动作：在 `word-learning-entrance.tsx` 中调用 `/api/adaptive/recommendation/<user>`，根据 VKS 选项构造上下文，自动更新 `currentWordId`、推荐模块并展示状态；失败时回退到默认路径并记录追踪事件。
- 动作：新增 `simulate_learning_records.js`，可批量调用阶段二 API 生成学习会话/练习记录，为推荐与复习逻辑提供演示数据。
- 验证：`npx eslint` 覆盖更新文件；可通过 `node simulate_learning_records.js` 预填充数据后，重新访问 VKS 入口查看推荐结果。
- 下一步：Step 4 扩展词汇与题目数据，或构建复习入口 UI，使推荐输出与真实内容联动。

### Step 4：扩充词汇数据与复习流程（阶段性完成）
- 动作：整理原始 PPT 资料，开发 `parse_learning_materials.py` 将 `词汇学习资料/*.pptx` 解析为结构化 JSON，统一归档到 `data/learning_materials/` 并生成 `_manifest.json`，为后续导入数据库或前端展示提供素材。
- 动作：升级 `generate_test_data.py` 支持批量造数，覆盖多类型用户、词汇、会话、练习与事件，便于阶段二数据库快速填充演示数据。
- 动作：在 `learning-dashboard-simple.tsx` 中新增 Review 区块，调用 `/api/review/user/<id>/due` 展示待复习队列，并与智能推荐卡片串联；同时把 Dashboard 依赖的 `/api/analytics/user/.../dashboard` 全量字段可视化。
- 动作：新增 `GET /api/learning/word/<id>/exercises`，基于阶段二数据库动态组合释义/搭配/例句题目并统一生成选项；执行 `generate_test_data.py` 补充 100 词汇与 1.5 万条练习记录后，用 Flask `test_client()` 验证接口返回结构。
- 动作：改造 `pages/exercise.tsx`，按推荐上下文读取词汇 ID 调用上述接口，处理加载/错误状态、自动回写时间追踪 (`/api/learning/exercise/record`)，并用 `npx eslint` 校验前端改动。
- 现状：练习页现已依据推荐结果加载动态题组并沉淀练习记录，自适应回路打通；题干仍来源于脚本生成的示例（未整合 PPT 原始题目/音频），多题型配置与判题细则尚待补全。
- 下一步：将 `data/learning_materials/*.json` 中的真实内容映射到练习接口（含音频、多选/填空配置），并依据算法输出调节题量、难度与复习排期，实现更丰富的反馈与评估。

### Step 5：用户体系与配置管理（进行中）
- 动作：新增 `LearningProvider`（`src/context/LearningContext.tsx`）集中管理 `userId` 与 API Base URL override，统一落地 localStorage，并导出 `useLearningContext` 供页面复用。
- 动作：在 `_app.tsx` 引入全局 `UserSwitcher` 浮层，支持从测试账号列表快速切换或输入自定义 ID，同时可即时调整 `NEXT_PUBLIC_API_BASE_URL` 覆盖值。
- 动作：改造 `word-learning-entrance.tsx`、`exercise.tsx`、`learning-dashboard-simple.tsx` 使用上下文驱动 API 调用与时间追踪，将用户相关状态按 `learning:<userId>:*` 前缀分区存储，避免多账号互相污染。
- 动作：调整 `src/lib/apiClient.ts` 支持运行期 Base URL 覆写，并在 `运行指南.md` 补充环境变量说明与多账号切换指引。
- 动作：后端新增 `GET /api/users`（`app_phase2.py`），聚合 `user_profile` 与 `user_progress` 信息；前端 `LearningContext` 自动拉取并缓存用户列表，UserSwitcher 支持手动刷新与近期词汇显示。
- 动作：实现 `LearningSessionProvider` 管理 per-user 学习上下文（词汇、模块、VKS 等），统一持久化到 `learningSession:<user>`；`word-learning-entrance.tsx`、`exercise.tsx` 等页面改为消费该上下文。
- 动作：Dashboard 顶部增加“当前学习上下文”卡片展示 session 状态并提供“继续学习”入口；UserSwitcher 新增“清除当前会话数据”操作，便于在调试时快速归零；另新增“最近学习会话”列表，支持快速跳转历史模块。
- 现状：核心学习/练习/分析链路已可在多账号间无缝切换，前后端均遵循上下文 `userId`；API Base URL 可在开发时动态指向不同后端实例，用户列表亦能从数据库实时获取。
- 下一步：基于会话上下文扩展“最近学习词/时间”概览（结合 `learning_session` 表聚合多维数据），在 Dashboard/入口提供个性化提示，并探索轻量授权（例如账号标签与只读模式）。
